///ads_load_interstitial(ads_provider)

switch argument0 {
case "AdMob" :
    /*
    if (string(GoogleMobileAds_InterstitialStatus()) != "Ready" && global.interLoading == false) {
    
        GoogleMobileAds_LoadInterstitial();
        global.interLoading = true;
    }
    */    
    break;
    
}
